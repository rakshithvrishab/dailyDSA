class Solution {
    public int rowWithMax1s(int A[][]) {
        for(int col=0;col<A[0].length;col++) {
            for(int row=0;row<A.length;row++) {
                if(A[row][col]==1){
                    return row;
                }
            }
        }     
        return -1;
    }
}
